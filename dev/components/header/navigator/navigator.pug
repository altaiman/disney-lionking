include data.pug

mixin navigator
  +b.NAV.navigator
    +e.UL.list
      each item in dataNavigator
        +e.LI.item
          +e.A.link(href="#")= item
    +e.A.link(href="#")
      +e.DIV.link-ico
        +svg({
          name: 'ico-auth'
        })
