mixin prize(data)
  +b.prize
    +e.image(style="background-color: "+data.color+";")
      img(src=global.paths.pictures+'prizes/'+data.img)
    +e.caption
      span!= data.caption
    +b.BUTTON.button(data-modal-open="prize")
      span Подробнее
